@model IEnumerable<CS322_PZ_V01_BojanPetrovic2745.Models.Kontrola>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.kontrola1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.kontrola1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDkon">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDkon">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDkon">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
